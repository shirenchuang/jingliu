# 参与贡献

感谢你愿意帮助改进静流。

## 开始之前

- Bug 与功能建议优先使用对应的 Issue 模板。
- 新平台适配、权限扩大、数据上传或默认行为变化，请先开 Issue 讨论。
- 不要提交真实 API Key、Cookie、私信、未脱敏推文或其他个人数据。

## 开发流程

1. Fork 仓库并创建一个用途明确的分支。
2. 在 `extension/` 中安装依赖：`npm install`。
3. 修改代码并补充相应测试。
4. 运行 `npm run check`、`npm test` 和 `npm run test:e2e`。
5. 提交 Pull Request，说明动机、行为变化和验证方式。

## 设计原则

- 本地优先：能在浏览器本地完成的判断不发送到外部。
- 用户控制：规则、服务商和密钥由用户选择。
- 可逆过滤：默认折叠而不是删除。
- 最小权限：只申请实现当前功能所需的权限。
- 明确边界：新增数据读取或网络请求必须同步更新隐私文档。

## 代码约定

- 当前代码使用原生 JavaScript、HTML 和 CSS，不引入运行时框架。
- 保持 Manifest V3 兼容。
- 平台 DOM 细节放入对应适配器，通用决策逻辑放在 `core.js`。
- 用户可见文本优先使用清晰、直接的中文。
