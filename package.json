{
  "name": "jingliu",
  "version": "0.6.0",
  "private": true,
  "description": "Local-first X feed filtering Chrome extension powered by user-owned Jev.",
  "scripts": {
    "check": "npm --prefix extension run check",
    "test": "npm --prefix extension test",
    "test:e2e": "npm --prefix extension run test:e2e",
    "test:all": "npm --prefix extension run test:all",
    "package": "./scripts/package-extension.sh"
  }
}
