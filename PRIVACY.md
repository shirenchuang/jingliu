# 静流隐私说明

静流是本地优先的 Chrome 扩展，只在用户打开的 X / Twitter 页面中工作。

## 读取的数据

- 当前页面已经载入的公开推文文字
- 推文作者账号
- X 显示的内容标签，例如 `Ad`、`Promoted`、`广告`

静流不读取私信、密码、Cookie、表单输入，也不主动抓取没有出现在当前页面里的历史内容。

## 数据处理

作者名单、硬过滤词和平台广告标签在浏览器本地判断。只有不能被本地规则直接判断的公开内容，才会发送到用户在设置页指定的 TypeSafe、OpenRouter 或自定义 Jev 接口。请求不经过静流服务器。

## 本地存储

过滤偏好、接口地址、模型名称和 API Key 保存在 `chrome.storage.local` 中。静流目前不包含账号、遥测、分析 SDK 或自建中转服务器。

过滤记录只包含运行时间、处理数量、折叠数量和原因类别；误判反馈只包含来源、动作和原因类别。两者都不保存推文原文、作者或链接。卸载扩展后，Chrome 会按其机制清理扩展本地存储。

用户主动导出的规则备份仅包含过滤政策、作者名单、显示方式和判断参数；不包含 API Key、接口地址、历史记录、反馈记录或推文内容。

## 官网

官网是静态页面，不提供账号、表单或云同步。托管平台可能为提供网络服务而处理基础访问日志，具体以托管平台政策为准。
